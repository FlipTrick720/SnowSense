# .gitlab-ci.yml

stages:
  - test

fitness-functions:
  stage: test
  image: maven:3.9-eclipse-temurin-17
  script:
    - cd notification-backend
    - mvn test -Dtest=ServiceCouplingFitnessFunctionTest --no-transfer-progress
